package July15;

public class StudentMain {
    public static void main(String[] args) {

        Student student = new Student();
        // student --> object reference
        // new --> keyword, gives memory to object
        // student() -->constructor, initialize object

        student.writing();
        student.study();
        student.laziness();
        student.sum(1,2);
    }
}
